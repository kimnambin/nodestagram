<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
  <script src = "/js/storydetail"></script>
  

</head>
<body>
  
  <div class="card">
    <!--카드 컨테이너의 상단부분-->
    <div class="card-body">
        <% storyposts.forEach((storypost, index) => { %>
            <div class="story-card">
                <h5 class="card-title" style="display: flex; align-items: center; margin-right: auto;">
                    <img src="/images/profile.jpg" style="height: 25px; width: 25px; margin-right:10px;">
                    <p style="margin: 0;"><%= storypost.userId %></p>
                    <a href="/mainupload" style="text-decoration: none; color: black; font-size: 30px;  margin-left: 320px;"> x </a>
                </h5>
                <hr style="width: 100%; height: 1px; background-color: #dbdbdb; border: none; margin: 5px 0;">
        
                <!--카드 컨테이너의 메인부분-->
                <h6 class="card-subtitle mb-2 text-muted">
                    <div class="image-container" style="position: relative;">
                        <button class="left-btn" type="button"><span class="material-icons">chevron_left</span></button>
                        <% if (files2[index]) { %> <!-- 해당 인덱스에 대한 파일이 존재하는 경우에만 실행 -->
                            <img src="/storys/<%= files2[index] %>" style="height: 460px; width: 380px; margin-right:10px;"><br>
                        <% } %>
                        <button class="right-btn" type="button"><span class="material-icons">chevron_right</span></button>
                    </div>

                   
                    <!--하트는 클릭하면 변하게 나중에 수정필요-->
                    <img src="/images/heart.png" style="width:25px; height: 25px;">
                </h6>
            </div>
        <% }) %>
    </div>
</div>


  
    <style>
        body, html {
    height: 100%;
    margin: 0;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  /* card 요소를 중앙에 배치 */
  .card {
    align-items: center;
    background-color: #ffffff;
    display: flex;
    flex-direction: column; 
    border: solid 0.1rem #dbdbdb;
    border-radius: 0.1rem;
    height: 650px;
    width: 500px; 
  }

  .card-body{
    display: flex; 
    flex-direction: column; 
    justify-content: center; 
    align-items: center;
  }

  .left-btn, .right-btn {
        position: absolute;
        top: 50%;
        transform: translateY(-50%);
        border: none;
        cursor: pointer;
        padding: 0;
        border-radius: 0.8rem;
      }

      .left-btn {
        left: -40px;
      }

      .right-btn {
        right: -40px;
      }

 

    </style>
    
</body>
</html>
